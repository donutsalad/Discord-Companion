{
  "name": "create_record",
  "description": "This creates an embedding that is stored in a database for quick recall later using cosine similarity.",
  "strict": false,
  "parameters": {
    "type": "object",
    "properties": {
      "Abstract": {
        "type": "string",
        "description": "All of the information required for the record should be put here in a way it can be embedded semantically."
      },
      "Memory": {
        "type": "string",
        "description": "Whatever the user wants to store, a URL, a discord message link, or simply a plain text result."
      }
    },
    "required": [
      "Abstract",
      "Memory"
    ]
  }
}