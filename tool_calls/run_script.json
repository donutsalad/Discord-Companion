{
  "name": "run_script",
  "description": "Executes a saved script using the given arguments and captures the resulting output.",
  "strict": false,
  "parameters": {
    "type": "object",
    "properties": {
      "Script Name": {
        "type": "string",
        "description": "The name of the script to be executed. Search scripts using list_scripts if the name is not found."
      },
      "Arguments": {
        "type": "string",
        "description": "A string of arguments to pass to the script, formatted like 'value1 value2'."
      }
    },
    "required": [
      "Script Name",
      "Arguments"
    ]
  }
}